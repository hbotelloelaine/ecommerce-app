<section>
  <div class="row">
    <div class="col-sm-9">
      <div class="product_single">
        <h2><%= @product.sale_message %></h2>
        <h2><%= @product.name %></h2>f
        <p>Price: <%= @product.price %></p>
        <p>Tax: <%= @product.tax %></p>
        <p>Total: <%= @product.total %></p>
        <% @product.images.each do |image| %>
        <img src="<%= image.image_url %>" alt="<%= @product.name %>" class="img-responsive">
        <%end%>
        <p>Description: <%= @product.description %></p>
        <p>Supplier: <%= @product.supplier.name %></p>

<!--       <h2><% @product.price_asc %></h2>
  <h2><% @product.price_desc %></h2> -->
  <div>
    <%= f.label @carted_products do |f| %>f
    <%= label_tag :quantity %>
    <%= text_field_tag :quantity, @carted_product.quantity %>
  </div>
  <div>
    <%= hidden_field_tag :product_id, @product.id %>
  </div>

<div>
  <%= submit_tag "Add To Cart" %>
</div>
<% end %>
<%= link_to "Delete", "/products/#{@product.id}", {method: :delete, class: "bnt btn-danger"} %>
<%= link_to "Edit","/products/#{@product.id}/edit", class: "btn btn-success" %>
</div>


</div>
</div>
</section>

