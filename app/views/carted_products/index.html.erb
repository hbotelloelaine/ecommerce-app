<table class="table">
  <tr>
    <th>Product Name</th>
    <th>Quantity</th>
    <th>Price Per Product</th>
  </tr>
  <%@carted_product.each do |carted_product| %>
  <tr>
    <td><%=carted_product.product.name %></td>
    <td><%=carted_product.quantity %></td>
    <td><%=carted_product.product.price %></td>
    <td><%= link_to "Remove Product(s)","/carted_products/#{carted_product.id}", method: :delete %></td>
  </tr>
  <% end %>
</table>
<table class="table">
  <tr>
    <th>Subtotal</th>
    <th><%= @subtotal %></th>
    <th>Tax</th>
    <th><%= @tax %></th>
    <th>Total</th>
    <th><%= @total %></th>
  </tr>
</table>

<%= link_to "Confirm", "/orders", method: :post %>